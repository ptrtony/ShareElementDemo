<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="activityInnerAnim">
        <item name="android:windowEnterTransition">@transition/activity_explode</item>
        <!--<item name="android:windowEnterAnimation">@transition/activity_explode</item>-->
        <item name="android:windowExitTransition">@transition/activity_explode</item>
        <!--<item name="android:windowExitAnimation">@transition/activity_explode</item>-->
    </style>

    <style name="Transparent" parent="AppTheme">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
    </style>
</resources>
